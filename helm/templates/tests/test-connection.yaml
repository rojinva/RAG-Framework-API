apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ent-openai-lambots-api-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ent-openai-lambots-api-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ent-openai-lambots-api-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
